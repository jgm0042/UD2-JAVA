package Variables;

public class Variables3 {
    public static void main(String[] args) {
        double x = 5, y = 2, a = 3, b = 4, c = 2, d = 6, e = 1, f = 7, g = 3, h = 2, j = 1, m = 8, n = 4, p = 3, q = 1, r = 2, s = 5;

        double resultA = (3.0 / 2.0) + (4.0 / 3.0);
        double resultB = (1.0 / (x - 5)) - (3 * x * y / 4);
        double resultC = (1.0 / 2.0) + 7;
        double resultD = 7 + (1.0 / 2.0);
        double resultE = (Math.pow(a, 2) / (b - c)) + ((d - e) / ((f - g * h) / j));
        double resultF = (m / n) + p;
        double resultG = (m + n) / (p - q);
        double resultH = (Math.pow(a, 2) / Math.pow(b, 2)) + (Math.pow(c, 2) / Math.pow(d, 2));
        double resultI = (m + n) / p - (q - r) / s;
        double resultJ = (3 * a + b) / (c - (d + 5 * e)) / (f + g) / (2 * h);
        double resultK = (Math.pow(a, 2) + 2 * a * b + Math.pow(b, 2)) / (1.0 / (Math.pow(x, 2) + 2));

    }
}
